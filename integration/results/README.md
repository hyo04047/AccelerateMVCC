# Multi-run measurement results (Phase 3 gate: variance/error-bars + raw-log archival)

Raw per-run mysqld/scan logs and per-config CSVs land here. Generated by integration/scripts/build_q11_*.sh.
Each CSV: one row per run; summary stats (median/min/max) computed at the bottom or in the session report.
